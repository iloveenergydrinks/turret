import { IsolatedEarnPage } from "@/src/screens/IsolatedMarketScreen/IsolatedEarnPage";
import { Suspense } from "react";

export default function Page() {
  return <Suspense><IsolatedEarnPage /></Suspense>;
}
