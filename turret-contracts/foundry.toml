[profile.default]
src = "."
out = "out"
cache_path = "cache"
solc_version = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
auto_detect_remappings = false
remappings = ["openzeppelin-contracts/=openzeppelin-contracts/", "reviewed/=reviewed/"]
