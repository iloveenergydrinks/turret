[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyType = "ALWAYS"
overlapSeconds = 0
drainingSeconds = 20
